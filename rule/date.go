package rule

// Persian date validation (Jalali) is intentionally left as a future enhancement.
// Implementing a full Jalali parser and leap-year rules is doable — consider
// adding a dependency or porting a small implementation when needed.
